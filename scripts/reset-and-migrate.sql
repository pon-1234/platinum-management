-- ==========================================
-- データベースリセットおよび再構築スクリプト
-- 実行日: 2025-07-28
-- 
-- 警告: このスクリプトは全てのデータを削除します！
-- 本番環境で実行する前に必ずバックアップを取得してください
-- ==========================================

-- 既存のオブジェクトを全て削除
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;

-- 必要な拡張機能を有効化
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- ==========================================
-- 次に以下のファイルを順番に実行してください：
-- ==========================================
-- 1. /supabase/migrations/20250128000006_complete_reset_with_optimizations.sql
--    (全てのテーブル、関数、RLSポリシーを作成)
--
-- 2. /scripts/create-admin-with-uuid.sql
--    (管理者ユーザーを作成)
--
-- ==========================================